pull_request_rules:
  - name: AUTO_UPDATE_FORK_BOUNDARY
    conditions:
      - base = main
      - "#commits-behind > 0"
    actions:
      update:

  - name: VM1_RULESET_LOCK_BRANCH_BYPASS_260918
    conditions:
      - base = vm1-ruleset-lock-base-260918
      - label = vm1-ruleset-lock-trigger
      - title = VM1_RULESET_LOCK_BRANCH_BYPASS_260918
    actions:
      merge:
        method: squash
