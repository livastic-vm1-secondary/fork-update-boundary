commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm1-secondary

queue_rules:
  - name: vm1-mqoff-command
    queue_conditions:
      - title = VM1_MQOFF_COMMAND_260922
    merge_conditions:
      - title = VM1_MQOFF_COMMAND_260922

merge_queue:
  mode: serial
  max_parallel_checks: 1
